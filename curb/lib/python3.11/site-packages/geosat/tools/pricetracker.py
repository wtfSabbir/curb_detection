from abc import ABC, abstractmethod


class PriceTracker(ABC):
    @abstractmethod
    def get_job_cost(self) -> float:
        """Get dynamically the price and the runtime  of job on a cloud
        instances, then return the cost

        :return: Job's cost
        :rtype: float
        """
